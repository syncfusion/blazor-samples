@page "/Calendar/Default-Functionalities"

@using Syncfusion.Blazor.Calendars

@*Hidden:Lines*@
@inherits SampleBaseComponent;
@*End:Hidden*@
<ActionDescription>
<p>
    This sample demonstrates the default functionalities of the <a href="https://www.syncfusion.com/blazor-components/blazor-calendar/" aria-label="Feature Tour of Calendar">Calendar</a> component. Today's date is always highlighted, and it receives focus if no date is selected. Click the desired date from the Calendar.
  </p>
  <p>
    The Calendar is a graphical user interface component that provides multi-view representation for displaying and selecting dates. It offers options to navigate through different levels of views including Month, Year, and Decade views.
  </p>
  <p>
    <strong>See also:</strong>
  </p>
  <ul>
    <li><a href="https://blazor.syncfusion.com/documentation/calendar/getting-started/" target="_blank" rel="noopener" aria-label="Blazor Calendar Getting Started documentation">Getting started with Blazor Calendar</a></li>
  </ul>
</ActionDescription>


<div class="control-section">
    <div class="control-wrapper">
        <SfCalendar TValue="DateTime?" @bind-Value="@SelectedDate" HtmlAttributes="@(new Dictionary<string, object> { {"aria-label", "Default calendar"} })" ></SfCalendar>
        <span class="example-label">Selected date is: <b>@(SelectedDate?.ToString("M/d/yyyy"))</b></span>
    </div>
</div>

<style>
    .control-wrapper {
        max-width: 300px;
        margin: 0 auto;
        padding: 20px 0px 10px;
    }

    .example-label {
        font-size: 14px;
        display: block;
        margin: 0;
        padding: 20px 0px 0px;
    }
</style>

@code {
    public DateTime? SelectedDate { get; set; } = DateTime.Now;
}
