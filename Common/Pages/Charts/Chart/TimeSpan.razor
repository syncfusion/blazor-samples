
<span id="information">
    &nbsp;&nbsp; Time Taken : <b>@Timediff</b>
</span>

@code {
    [Parameter]
    public string Timediff { get; set; }

    public void UpdateTimer(string timediff)
    {
        Timediff = timediff;
        StateHasChanged();
    }
}
