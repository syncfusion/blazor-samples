@page "/dialog/resizable"

@using Syncfusion.Blazor.Popups

@*Hidden:Lines*@
@inherits SampleBaseComponent
@*End:Hidden*@

<ActionDescription>
<p>
    This sample demonstrates how to resize the Blazor Dialog component in all directions. To resize the modal Dialog, select and resize it using its handle (grip) or hover on any edges or borders of the Dialog. The "Open" button is used to reopen the Dialog if it is closed.
  </p>
  <p>
    Users can create resizable modal Blazor Dialog by setting the <code>EnableResize</code> property to <code>true</code>, which enables users to change the Dialog size dynamically and view its content in expanded mode. The <code>ResizeHandles</code> property can be configured to specify the directions in which the Dialog should resize. When you configure the <code>Target</code> property along with <code>EnableResize</code> property, the Dialog can be resized within its specified target container.
  </p>
</ActionDescription>


<div class="col-lg-12 control-section" id="target">
    <div>
        @if (this.ShowButton)
        {
            <button class="e-btn" @onclick="@OnClicked" aria-label="Open Resizable Dialog">Open</button>
        }
    </div>
    <SfDialog Target="#target" Width="300px" ResizeHandles="@_dialogResizeDirections" AllowDragging="true" EnableResize="true" ShowCloseIcon="true" @bind-Visible="Visibility">
        <DialogTemplates>
            <Header> Resize Me!!!</Header>
            <Content> This is a dialog with resizable support. </Content>
        </DialogTemplates>
        <DialogEvents OnOpen="@DialogOpen" Closed="@DialogClose"></DialogEvents>
    </SfDialog>
</div>

<style>
    #target {
        height: 500px;
    }
</style>

@code {
    private bool Visibility { get; set; } = true;
    private bool ShowButton { get; set; } = false;
    private ResizeDirection[] _dialogResizeDirections { get; set; } = new ResizeDirection[] { ResizeDirection.All };
    
    private void DialogOpen(Object args)
    {
        this.ShowButton = false;
    }

    private void DialogClose(Object args)
    {
        this.ShowButton = true;
    }

    private void OnClicked()
    {
        this.Visibility = true;
    }
}