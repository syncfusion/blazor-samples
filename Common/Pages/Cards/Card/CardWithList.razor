@page "/card/card-with-list"

@using Syncfusion.Blazor.Cards
@using Syncfusion.Blazor.Lists
@*Hidden:Lines*@
@inherits SampleBaseComponent;
@*End:Hidden*@
<ActionDescription>
  <p>This sample demonstrates rendering a List component inside a Card container to present list-based content in a compact, structured layout. The example shows how Card templates can host interactive or read-only lists while preserving visual grouping and contextual framing for related items.</p>

  <p>The Card acts as a small, flexible, and extensible container that enforces consistent spacing, header/body structure, and optional footer actions. Embedding a List within the Card template provides an organized presentation that improves scannability and aligns list items with surrounding metadata or controls.</p>

  <p>Rendering the List as a Card template supports customization at several levels: define an ItemTemplate to control per-item markup, include a Card header for title and contextual details, and use the Card body to contain the List so it inherits padding and boundary styling. This approach keeps markup modular and simplifies reuse across the application.</p>

  <ul>
    <li>Maintain consistent visual grouping for related entries.</li>
    <li>Use ItemTemplate to include avatars, captions, or secondary text.</li>
    <li>Leverage Card header/footer for summary information or actions.</li>
    <li>Keep list markup semantic and accessible for assistive technologies.</li>
  </ul>

  <p>Implementation notes: bind the List data source to the view model and use ItemTemplate to format each entry. Ensure accessible attributes (labels, roles, and keyboard focus) are preserved when composing templates. Test the Card/List composition across viewport sizes and with screen readers to confirm readable layout, focus order, and interactive behavior.</p>
</ActionDescription>


<div class="control-section">
    <div class="row">
        <div class="col-lg-6">
            <SfCard>
                <CardContent>
                    <SfListView Id="List" DataSource="@ListData" CssClass="e-list-template" SortOrder="Syncfusion.Blazor.Lists.SortOrder.Ascending">
                        <ListViewFieldSettings TValue="DataModel" Id="Id" Text="Text"></ListViewFieldSettings>
                        <ListViewTemplates TValue="DataModel">
                            <Template>
                                <div class="e-list-wrapper e-list-multi-line e-list-avatar">
                                    @if (((context as DataModel).Avatar) != "")
                                    {
                                        <span class="e-avatar e-avatar-circle">@((context as DataModel).Avatar)</span>
                                    }
                                    else
                                    {
                                        <span class="@((context as DataModel).Pic) e-avatar e-avatar-circle"> </span>
                                    }
                                    <span class="e-list-item-header">@((context as DataModel).Text)</span>
                                    @if (((context as DataModel).Contact) == "Online")
                                    {
                                        <span class="e-list-content e-card-online">@((context as DataModel).Contact)</span>    
                                    }
                                    else
                                    {
                                        <span class="e-list-content e-card-offline">@((context as DataModel).Contact)</span>    
                                    }
                                    
                                </div>
                            </Template>
                        </ListViewTemplates>
                    </SfListView>
                </CardContent>
            </SfCard>
        </div>
    </div>
</div>

@code{

    private List<DataModel> ListData = new List<DataModel>();
    
    protected override void OnInitialized()
    {
        base.OnInitialized();
        
        ListData.Add(new DataModel { Text = "Fua Lamba ", Contact = "Online", Id = "5", Avatar = "", Pic = "avatar-profile-1" });
        ListData.Add(new DataModel { Text = "Harrisburg Keith", Contact = "Offline", Id = "1", Avatar = "", Pic = "avatar-profile-2" });
        ListData.Add(new DataModel { Text = "Izumi Katsuyoshi", Contact = "Online", Id = "2", Avatar = "", Pic = "avatar-profile-3" });
        ListData.Add(new DataModel { Text = "Nawf El Azam", Contact = "Offline", Id = "9", Avatar = "", Pic = "avatar-profile-4" });
        ListData.Add(new DataModel { Text = "Sofietje Boksem", Contact = "Online", Id = "7", Avatar = "", Pic = "avatar-profile-5" });
    }

    public class DataModel
    {
        public string? Id { get; set; }
        public string? Text { get; set; }
        public string? Contact { get; set; }
        public string? Avatar { get; set; }
        public string? Pic { get; set; }
    }
}

<style>
    .e-listview.e-control {
        width: 400px;
    }

    .avatar-profile-1 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-one.png"));
    }

    .avatar-profile-2 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-two.png"));
    }

    .avatar-profile-3 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-four.png"));
    }

    .avatar-profile-4 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-six.png"));
    }

    .avatar-profile-5 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-seven.png"));
    }

    .material3 #List .avatar-profile-1,
    .material3-dark #List .avatar-profile-1 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-one.png"));
        background-size: cover;
    }

    .material3 #List .avatar-profile-2,
    .material3-dark #List .avatar-profile-2 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-two.png"));
        background-size: cover;
    }

    .material3 #List avatar-profile-3,
    .material3-dark #List .avatar-profile-3 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-four.png"));
        background-size: cover;
    }

    .material3 #List .avatar-profile-4,
    .material3-dark #List .avatar-profile-4 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-six.png"));
        background-size: cover;
    }

    .material3 #List .avatar-profile-5,
    .material3-dark #List .avatar-profile-5 {
        background-image: url(@(SampleService?.WebAssetsPath + "images/cards/profile-seven.png"));
        background-size: cover;
    }

    #List .e-list-item:nth-child(1) .e-avatar {
        background-color: #039be5;
    }

    #List .e-list-item:nth-child(2) .e-avatar {
        background-color: #e91e63;
    }

    #List .e-list-item:nth-child(4) .e-avatar {
        background-color: #e91e63;
    }

    #List .e-list-item:nth-child(3) .e-avatar {
        background-color: #039be5;
    }

    #List .e-list-item:nth-child(6) .e-avatar {
        background-color: #009688;
    }

    #List .e-list-item:nth-child(8) .e-avatar {
        background-color: #000088;
    }

    .e-list-item-header {
        font-family: HelveticaNeue-Bold, 'Segoe UI' !important;
        font-size: 14px !important;
        color: #000000 !important;
    }

    #List .e-list-content:nth-child(odd) {
        font-family: Helvetica, 'Segoe UI' !important;
        font-size: 12px !important;
    }

    .e-card-online {
        color: #39D321 !important;
    }

    .e-card-offline {
        color: red !important;
    }

    #List .e-list-content:nth-child(4) {
        font-family: Helvetica, 'Segoe UI' !important;
        font-size: 12px !important;
        color: #000 !important;
    }

    #List .e-list-content:nth-child(2) {
        font-family: Helvetica, 'Segoe UI' !important;
        font-size: 12px !important;
        color: #000 !important;
    }

    .e-card .e-card-content {
        padding: 0px;
    }

    .e-listview.e-control {
        width: inherit;
    }

    .row {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .bootstrap-dark .e-card {
        border: 1px solid #6e6e6e;
    }
    .bootstrap-dark .e-card:active,
    .bootstrap-dark .e-card:focus,
    .bootstrap-dark .e-card:hover {
        border-color: #6e6e6e;
    }
    .material-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .material3-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .fabric-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .bootstrap-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .tailwind-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .tailwind3-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .highcontrast .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header {
        color: #dadada !important;
    }
    .highcontrast .e-listview.e-list-template .e-focused .e-list-wrapper.e-list-multi-line .e-list-item-header {
        color: #000 !important;
    }
    
    .fluent-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .fluent2-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .fluent2-highcontrast .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header,
    .bootstrap5-dark .e-listview.e-list-template .e-list-wrapper.e-list-multi-line .e-list-item-header {
        color: #fff !important;
    }
</style>
