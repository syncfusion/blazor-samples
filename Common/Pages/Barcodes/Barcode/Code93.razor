@page "/barcodes/code-93"

@using Syncfusion.Blazor.BarcodeGenerator
@*Hidden:Lines*@
@inherits SampleBaseComponent;
@inject NavigationManager NavigationManager




@*End:Hidden*@

<SampleDescription>
    <p>This sample visualizes how to present a model number with higher density and data security enhancements compared to Code 39. Code 93 has a native character set of 26 uppercase letters, 10 digits, and 7 special characters.</p>
</SampleDescription>
<ActionDescription>
   <p>This sample shows how to create a Code 93 barcode with two check characters. The <code>Type</code> property can be used to set the barcode type as <code>Code93</code>. By default, <code>EnableCheckSum</code> is set as <code>true</code>, but the checksum can be disabled by assigning <code>false</code> to it.</p>
</ActionDescription>

<div class="control-section" style="width: 100%;height: 590px;">
    <div class="center">
        <div class="centercontrol">
            <SfBarcodeGenerator Width="200px" Height="150px" Type="@BarcodeType.Code93" Value="ABCD-123-/+"></SfBarcodeGenerator>
        </div>
    </div>
    <div class="allowedText">
        <span style="font-weight: 600;"> Allowed input characters:</span> The Code 93 barcode supports all numeric digits(0-9), uppercase alphabetic letters(A-Z), and symbols(-, ., $, /, +, %, SPACE).
    </div>
</div>

<style>
    .centercontrol {
        margin: auto;
        width: 50%;
        padding-left: 18%;
        height: 50%;
    }

    .allowedText {
        margin-left: 14px;
        margin-right: 17px;
        margin-top: 14px;
        width: auto;
        height: auto;
    }


    .center {
        margin-left: 14px;
        margin-right: 17px;
        margin-top: 14px;
        min-width: 280px;
        width: auto;
        border: 2px solid lightgray;
        min-height: 40%;
        padding-top: 35px;
    }

    @@media screen and (max-width: 570px)
    {
        .centercontrol{
            width: 60%;
            padding-left: 0px;
        }
    }
</style>


