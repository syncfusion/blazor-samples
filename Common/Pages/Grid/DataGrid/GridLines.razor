@page "/datagrid/grid-lines"

@using Syncfusion.Blazor.Grids
@using Syncfusion.Blazor.DropDowns

@*Hidden:Lines*@
@using BlazorDemos
@using blazor_griddata
@inherits SampleBaseComponent
@inject NavigationManager NavigationManager


@*End:Hidden*@

<ActionDescription>
<p>This sample demonstrates the DataGrid grid lines feature for controlling the visibility of row and column separators.</p>
  
  <p>The <a target='_blank' class='code' href='https://help.syncfusion.com/cr/blazor/Syncfusion.Blazor.Grids.SfGrid-1.html#Syncfusion_Blazor_Grids_SfGrid_1_GridLines' aria-label="Navigate to the GridLines property reference for DataGrid component">GridLines</a> property controls the visibility of lines that separate rows and columns in the grid. The available grid line display options are:</p>
  <ul>
    <li><strong>Default</strong> - Displays horizontal lines between rows</li>
    <li><strong>None</strong> - Hides all grid lines</li>
    <li><strong>Both</strong> - Displays both horizontal and vertical lines</li>
    <li><strong>Horizontal</strong> - Displays only horizontal lines between rows</li>
    <li><strong>Vertical</strong> - Displays only vertical lines between columns</li>
  </ul>
  
  <p>In this demo, the GridLines property is set to <strong>Both</strong>, displaying both horizontal and vertical separators for a clear grid structure.</p>
  
  <p>For more detailed information about grid lines configuration, refer to the <a target='_blank' href='https://blazor.syncfusion.com/documentation/datagrid/cell/#grid-lines' aria-label="Navigate to the documentation for Grid lines">grid lines documentation</a>.</p>
</ActionDescription>


<div class="col-lg-12 control-section">
    <div class="content-wrapper">
        <div class="row">
            <SfGrid DataSource="@GridData" AllowPaging="true" GridLines="GridLine.Both">
                <GridColumns>
                    <GridColumn Field=@nameof(OrdersDetails.OrderID) HeaderText="Order ID" TextAlign="TextAlign.Right"  Width="120"></GridColumn>
                    <GridColumn Field=@nameof(OrdersDetails.CustomerID) HeaderText="Customer ID" Width="150"></GridColumn>
                    <GridColumn Field=@nameof(OrdersDetails.Freight) TextAlign="TextAlign.Right"  Width="120" Format="C2"></GridColumn>
                    <GridColumn Field=@nameof(OrdersDetails.ShipCity) HeaderText="Ship City" Width="150"></GridColumn>
                    <GridColumn Field=@nameof(OrdersDetails.ShipCountry) HeaderText="Ship Country" Width="150"></GridColumn>
                </GridColumns>
            </SfGrid>
        </div>
    </div>
</div>

@code{

    public List<OrdersDetails>? GridData { get; set; }
    

    public class Lines
    {
        public string? id { get; set; }

        public string? text { get; set; }
    }

    private List<Lines> lines = new List<Lines>()
    {
        new Lines(){ id= "Default", text= "Default" },
        new Lines(){ id= "Horizontal", text= "Horizontal" },
        new Lines(){ id= "Vertical", text= "Vertical" },
        new Lines(){ id= "Both", text= "Both" },
        new Lines(){ id= "None", text= "None" },
    };

    protected override void OnInitialized()
    {
        
        GridData = OrdersDetails.GetAllRecords();
    }
}
